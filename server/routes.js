

// Board

const BOARD = "/boardServer";
const UPLOAD = "/upload";
const BOARDLIST = "/list";

const routes = {
    board: BOARD,
    upload: UPLOAD,
    boardList: BOARDLIST
};

export default routes;